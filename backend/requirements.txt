# Backend dependencies — minimum known-good versions, install with:
#   pip install -r requirements.txt
fastapi>=0.115.0
uvicorn>=0.30.0
gunicorn>=22.0.0
httpx>=0.27.0
pydantic-settings>=2.5.0
pymysql>=1.1.0
