"""Agent mode (T-A1): Strands Agents SDK layer on top of the native engine.

The agent orchestrates; the engine guarantees — geography, capacity and
closing-time feasibility are decided by the same deterministic services the
tool mode uses, never by the model. See agents/README in the repo wiki and
docs/ALGORITHM.md for the tool-layer contract.
"""
