# Base URL of the trip planner backend (no trailing slash).
# Development default is the local backend; set to '' (same origin) or an
# absolute URL when building for a deployment.
VITE_API_BASE_URL=http://localhost:5003
