# Secrets — never commit real keys
.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Frontend / Node
node_modules/
dist/

# Local data cache (SerpApi response cache)
cache/
data/poi_serpapi_cache/
data/osm_fee_cache/
data/lodging_serpapi_cache/

# macOS
.DS_Store

# Chinese docs kept local-only (English versions live in docs/)
docs-zh/

# DB seed data carries localized (CJK) catalog values — kept local only
db/seed_pois.sql
db/seed_lodgings.sql
db/create_lodgings.sql
